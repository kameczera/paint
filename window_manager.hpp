#include <SDL2/SDL.h>

class WindowManager {
    private:
        int screen_width;
        int screen_height;

    public:
        WindowManager(int screen_width, int screen_height) : screen_width(screen_width), screen_height(screen_height) {
            
        }
}